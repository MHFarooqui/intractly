const router = require('express').Router();



router.post('/createContact', (req,res) => {
    /**
     * call axios method to make request.
     */
});

/**
 * 
 * Add other routes.
 */

module.exports = router;